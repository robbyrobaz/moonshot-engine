# Moonshot Strategy Scout — 2026-03-25

## Tournament Analysis

### Current Champions
- **de44f72dbb01** (short, catboost): 0.68 FT PnL / 388 trades / 0.98 BT PF
  - Features: extended (37 features)
  - Params: lr=0.01, depth=8, n_est=100, neg_weight=8
- **new_listing** (rule_based): 0.0 FT PnL / 0 trades / 7.53 BT PF
  - Rule-based champion for new listings

### Retired Model Stats
- Total retired: 2,648 models (95% retirement rate — healthy)
- Top failure: `backtest_failed` (2,542 models, 96%)
- FT failures: unprofitable PF (<0.9 after 50 trades), low PF (<1.3 after 20 trades)
- Data quality issue: 10 models failed on `price_vs_52w_high` string conversion

### Model Type Distribution
- XGBoost: 1,196 models (35.9%)
- LightGBM: 1,149 models (34.5%)
- CatBoost: 1,119 models (33.6%)
- Rule-based: 1 model (0.03%)

### Feature Set Analysis
- **Extended (37 features)**: 490 models — includes funding, OI, mark, ticker, but NO social
- **Extended + Social (50 features)**: 437 models — full feature set
- **Core only (16 features)**: 232 models — minimal feature set
- **Core + extended (25 features)**: 217 models — no social
- **"all"**: 207 models — legacy tag

### Gaps Identified
1. **Underutilized features**: Social features (13 total) mostly untested in isolation
2. **Feature subsets**: No tests of ONLY volatility features, ONLY volume features, etc.
3. **Hyperparameter space**: Champion uses neg_class_weight=8 (extreme), but we haven't tested 4, 6, 10, 12
4. **Algo variety**: Only 1 rule-based model — no RandomForest, no ensemble stacking
5. **Direction balance**: No data on long/short/both split — check this
6. **Cross-feature interactions**: No polynomial features, no interaction terms

## Proposed Variants (10 models)

### 1. Volatility Specialist (Feature Subset)
**Hypothesis**: Volatility regime changes drive short-term reversals.
- **Features**: atr_percentile, atr_compression, high_low_range_pct, realized_vol_ratio, bb_squeeze_pct, btc_vol_percentile
- **Model**: XGBoost (lr=0.05, depth=5, n_est=150)
- **Direction**: short
- **Rationale**: Test if minimal volatility-only features can beat 37-feature models

### 2. Volume Momentum (Feature Subset)
**Hypothesis**: Volume leads price — pure volume signals may predict breakouts.
- **Features**: volume_ratio_7d, volume_ratio_3d, obv_slope, volume_spike, volume_trend, vol_24h_vs_7d_avg
- **Model**: LightGBM (lr=0.03, depth=6, n_est=200, num_leaves=31)
- **Direction**: long
- **Rationale**: Volume features unused in isolation; champion uses 37 features

### 3. Social Sentiment Pure (Feature Experiment)
**Hypothesis**: Social signals alone can predict hype-driven pumps.
- **Features**: ONLY 13 social features (fear_greed, coingecko_trending, news, reddit, github)
- **Model**: CatBoost (lr=0.02, depth=7, n_est=120)
- **Direction**: long
- **Rationale**: Social features never tested in isolation; may work for new listings

### 4. High Neg Weight CatBoost (Hyperparameter Sweep)
**Hypothesis**: Champion uses neg_weight=8; try 12 for even more conservative shorts.
- **Features**: extended (37 features, same as champion)
- **Model**: CatBoost (lr=0.01, depth=8, n_est=100, **neg_class_weight=12**)
- **Direction**: short
- **Rationale**: Push neg_weight higher to see if we can reduce false positives

### 5. Low Neg Weight XGBoost (Hyperparameter Sweep)
**Hypothesis**: Lower neg_weight may find more opportunities (higher recall).
- **Features**: extended (37 features)
- **Model**: XGBoost (lr=0.02, depth=6, n_est=150, **neg_class_weight=4**)
- **Direction**: short
- **Rationale**: Champion is conservative (neg=8); try aggressive (neg=4)

### 6. Deep CatBoost (Hyperparameter Sweep)
**Hypothesis**: Deeper trees may capture complex feature interactions.
- **Features**: extended + social (50 features)
- **Model**: CatBoost (lr=0.005, **depth=10**, n_est=200, neg_class_weight=6)
- **Direction**: both
- **Rationale**: Champion uses depth=8; test depth=10 with slower learning rate

### 7. Fast LightGBM (Hyperparameter Sweep)
**Hypothesis**: Higher learning rate + fewer estimators may generalize better.
- **Features**: core only (25 features, no social)
- **Model**: LightGBM (**lr=0.1**, depth=5, **n_est=50**, num_leaves=31)
- **Direction**: long
- **Rationale**: Fast learner, minimal features — test overfitting hypothesis

### 8. Price Structure Specialist (Feature Subset)
**Hypothesis**: Support/resistance + trend structure predicts reversals.
- **Features**: distance_from_support, distance_from_resistance, consec_down_bars, consec_up_bars, higher_highs, bb_position, momentum_4w, momentum_8w
- **Model**: XGBoost (lr=0.03, depth=6, n_est=100)
- **Direction**: short
- **Rationale**: Test pure price structure without volume/volatility noise

### 9. Funding Rate Contrarian (Feature Experiment)
**Hypothesis**: Extreme funding rates signal overextension → reversal.
- **Features**: funding_rate_current, funding_rate_7d_avg, funding_rate_extreme, oi_change_24h, oi_change_7d, oi_price_divergence, oi_percentile_90d
- **Model**: CatBoost (lr=0.02, depth=7, n_est=150, neg_class_weight=10)
- **Direction**: short
- **Rationale**: Funding/OI features underutilized; may predict funding-driven dumps

### 10. Minimal Core XGBoost (Feature Subset)
**Hypothesis**: Core 16 features may outperform kitchen-sink 50-feature models.
- **Features**: core only (16 features: price, momentum, volume, volatility, BTC regime, listing age)
- **Model**: XGBoost (lr=0.05, depth=5, n_est=100)
- **Direction**: both
- **Rationale**: Simplicity test — less is more, reduce overfitting

## Implementation Plan
1. Spawn 10 coding subagents (Codex), one per variant
2. Each writes JSON config to `blofin-moonshot-v2/configs/generated/2026-03-25-variant-N.json`
3. Insert model into `tournament_models` with `stage='backtest'`
4. Verify no pipeline breakage
5. Commit all configs to git
6. Next tournament cycle (4h timer) will pick them up

## Expected Outcomes
- 95% will retire (normal)
- 1-2 may reach FT
- 0-1 may become champion
- Goal: find ONE 0.5% winner
